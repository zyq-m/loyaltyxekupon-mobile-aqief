import { StyleSheet } from "react-native";

const insertPINstyle = StyleSheet.create({
  textHeader: {
    fontSize: 24,
    fontWeight: "600",
    textAlign: "center",
  },
});

export default insertPINstyle;
